# AIRD 품질진단 리포트 가이드

## 📋 문서 정보
- **버전**: 1.0
- **작성일**: 2025-11-28
- **대상**: 데이터 관리자, 분석가, 정책 담당자

---

## 🎯 품질진단의 중요성

### 왜 데이터 품질진단이 필요한가?

**"Garbage In, Garbage Out"**

AI/ML 모델의 성능은 입력 데이터의 품질에 직접적으로 의존합니다.  
아무리 뛰어난 알고리즘이라도 품질이 낮은 데이터로는 신뢰할 수 있는 결과를 얻을 수 없습니다.

### 품질진단의 효과

1. **모델 성능 향상**: 품질 높은 데이터 → 정확한 예측
2. **비용 절감**: 사전 오류 제거 → 재작업 방지
3. **신뢰도 제고**: 검증된 데이터 → 의사결정 신뢰
4. **규정 준수**: 데이터 거버넌스 기준 충족

---

## 📊 5대 품질 차원

### 1. 완전성 (Completeness)

**정의**: 데이터가 얼마나 완전한가?

**평가 항목**:
- 결측치 비율
- 필수 컬럼 존재 여부
- 빈 문자열 검사

**평가 기준**:
| 점수 | 등급 | 결측치 비율 | 평가 |
|------|------|------------|------|
| 90-100 | A | 0-5% | 우수 |
| 80-89 | B | 5-10% | 양호 |
| 70-79 | C | 10-20% | 보통 |
| 60-69 | D | 20-30% | 미흡 |
| 0-59 | F | 30%+ | 불량 |

**개선 방법**:
```python
# 결측치 보완 예시
df['제조시설면적'].fillna(df['제조시설면적'].median(), inplace=True)

# 필수 컬럼 추가
if '공장관리번호' not in df.columns:
    df['공장관리번호'] = df.index.astype(str)
```

---

### 2. 정확성 (Accuracy)

**정의**: 데이터가 현실을 얼마나 정확하게 반영하는가?

**평가 항목**:
- 날짜 형식 정확성
- 숫자 데이터 유효성 (음수, 이상치)
- 주소 형식 검증
- 코드 체계 준수

**평가 기준**:
| 점수 | 오류율 | 평가 |
|------|--------|------|
| 90-100 | 0-2% | 우수 |
| 80-89 | 2-5% | 양호 |
| 70-79 | 5-10% | 보통 |
| 60-69 | 10-15% | 미흡 |
| 0-59 | 15%+ | 불량 |

**개선 방법**:
```python
# 날짜 형식 표준화
df['최초승인일'] = pd.to_datetime(
    df['최초승인일'], 
    format='%Y%m%d', 
    errors='coerce'
)

# 음수 제거
df.loc[df['제조시설면적'] < 0, '제조시설면적'] = np.nan

# 주소 정제
df['공장주소'] = df['공장주소'].str.strip()
df['공장주소'] = df['공장주소'].str.replace(r'\s+', ' ', regex=True)
```

---

### 3. 일관성 (Consistency)

**정의**: 데이터가 내부적으로 얼마나 일관성 있는가?

**평가 항목**:
- 중복 레코드
- 데이터 형식 통일성
- 범주형 값 표준화
- Cross-field 검증

**평가 기준**:
| 점수 | 중복률 | 평가 |
|------|--------|------|
| 90-100 | 0-1% | 우수 |
| 80-89 | 1-3% | 양호 |
| 70-79 | 3-5% | 보통 |
| 60-69 | 5-10% | 미흡 |
| 0-59 | 10%+ | 불량 |

**개선 방법**:
```python
# 중복 제거
df = df.drop_duplicates(subset=['공장관리번호'], keep='first')

# 범주형 값 표준화
industry_mapping = {
    '제조업': '제조',
    '제 조': '제조',
    '製造': '제조'
}
df['업종명'] = df['업종명'].replace(industry_mapping)

# Cross-field 검증
# 종업원합계 = 남자 + 여자 + 외국인남자 + 외국인여자
employee_sum = (df['남자종업원'] + df['여자종업원'] + 
                df['외국인남자종업원'] + df['외국인여자종업원'])
df.loc[employee_sum != df['종업원합계'], '종업원합계'] = employee_sum
```

---

### 4. 적시성 (Timeliness)

**정의**: 데이터가 얼마나 최신인가?

**평가 항목**:
- 데이터 수집 시점
- 최근 갱신 비율
- 데이터 신선도

**평가 기준**:
| 점수 | 최신 데이터 비율 | 평가 |
|------|-----------------|------|
| 90-100 | 80%+ (최근 1년) | 우수 |
| 80-89 | 60-80% | 양호 |
| 70-79 | 40-60% | 보통 |
| 60-69 | 20-40% | 미흡 |
| 0-59 | 0-20% | 불량 |

**개선 방법**:
```python
# 최신 데이터 우선 사용
current_year = datetime.now().year
df['data_age'] = current_year - pd.to_datetime(df['등록일']).dt.year

# 오래된 데이터 필터링 (5년 이상)
df_recent = df[df['data_age'] <= 5]

# 정기 갱신 프로세스 수립
# - 분기별: 중요 데이터
# - 반기별: 일반 데이터
# - 연간: 참고 데이터
```

---

### 5. 유효성 (Validity)

**정의**: 데이터가 정의된 규칙을 얼마나 준수하는가?

**평가 항목**:
- 값의 유효 범위
- 논리적 관계 검증
- 참조 무결성

**평가 기준**:
| 점수 | 유효성 위반 | 평가 |
|------|------------|------|
| 90-100 | 0-2% | 우수 |
| 80-89 | 2-5% | 양호 |
| 70-79 | 5-10% | 보통 |
| 60-69 | 10-20% | 미흡 |
| 0-59 | 20%+ | 불량 |

**개선 방법**:
```python
# 값 범위 검증
valid_ages = (df['공장연령'] >= 0) & (df['공장연령'] <= 100)
df.loc[~valid_ages, '공장연령'] = np.nan

# 논리적 관계 검증
# 제조시설면적 + 부대시설면적 ≤ 건축면적
facility_sum = df['제조시설면적'] + df['부대시설면적']
invalid_area = facility_sum > df['건축면적'] * 1.1  # 10% 오차 허용
df.loc[invalid_area, ['제조시설면적', '부대시설면적']] = np.nan
```

---

## 📋 품질진단 프로세스

### Step 1: 데이터 로드 및 탐색

```python
import pandas as pd
import numpy as np

# 데이터 로드
df = pd.read_csv('seoul_factory_registry_2025_v1.csv')

# 기본 정보 확인
print(f"행 수: {len(df):,}")
print(f"열 수: {len(df.columns)}")
print(f"메모리 사용량: {df.memory_usage().sum() / 1024**2:.2f} MB")

# 데이터 타입
print(df.dtypes.value_counts())
```

### Step 2: 자동 품질진단 실행

```python
# AIRD 품질진단 노트북 실행
jupyter notebook aird_quality_diagnosis.ipynb
```

### Step 3: 리포트 분석

품질진단 실행 후 다음을 확인:
1. 종합 품질 점수
2. 차원별 세부 점수
3. 발견된 문제점 목록
4. 개선 권고사항

### Step 4: 개선 조치

우선순위에 따라 개선:
1. **High**: 완전성, 정확성 문제 (즉시 조치)
2. **Medium**: 일관성, 적시성 문제 (1주일 내)
3. **Low**: 유효성 문제 (1개월 내)

### Step 5: 재진단

개선 후 다시 품질진단 실행하여 효과 확인

---

## 📊 품질 리포트 해석

### 종합 점수별 조치

#### 90-100점 (A등급)
- **상태**: 우수
- **조치**: 현재 품질 유지
- **주기**: 분기별 재진단

#### 80-89점 (B등급)
- **상태**: 양호
- **조치**: 낮은 차원 개선
- **주기**: 월별 재진단

#### 70-79점 (C등급)
- **상태**: 보통
- **조치**: 개선 계획 수립 및 실행
- **주기**: 주별 모니터링

#### 60-69점 (D등급)
- **상태**: 미흡
- **조치**: 즉시 개선 필요
- **주기**: 일별 모니터링

#### 0-59점 (F등급)
- **상태**: 불량
- **조치**: 데이터 사용 보류, 전면 개선
- **주기**: 연속 모니터링

---

## 🛠️ 품질 개선 도구

### 1. 데이터 정제 도구

```python
class DataCleaner:
    """데이터 정제 유틸리티"""
    
    @staticmethod
    def remove_duplicates(df, key_column):
        """중복 제거"""
        before = len(df)
        df = df.drop_duplicates(subset=[key_column], keep='first')
        after = len(df)
        print(f"중복 제거: {before - after}개")
        return df
    
    @staticmethod
    def standardize_dates(df, date_columns, format='%Y%m%d'):
        """날짜 형식 표준화"""
        for col in date_columns:
            df[col] = pd.to_datetime(df[col], format=format, errors='coerce')
        return df
    
    @staticmethod
    def remove_outliers(df, column, method='iqr', threshold=3):
        """이상치 제거"""
        if method == 'iqr':
            Q1 = df[column].quantile(0.25)
            Q3 = df[column].quantile(0.75)
            IQR = Q3 - Q1
            lower = Q1 - threshold * IQR
            upper = Q3 + threshold * IQR
            df.loc[(df[column] < lower) | (df[column] > upper), column] = np.nan
        return df
```

### 2. 품질 모니터링 도구

```python
class QualityMonitor:
    """품질 지속 모니터링"""
    
    def __init__(self, df):
        self.df = df
        self.baseline_scores = {}
    
    def set_baseline(self):
        """기준선 설정"""
        self.baseline_scores = {
            'completeness': self._assess_completeness(),
            'accuracy': self._assess_accuracy(),
            'consistency': self._assess_consistency()
        }
    
    def check_degradation(self, df_new):
        """품질 저하 확인"""
        current_scores = {
            'completeness': self._assess_completeness(df_new),
            'accuracy': self._assess_accuracy(df_new),
            'consistency': self._assess_consistency(df_new)
        }
        
        alerts = []
        for dim, baseline in self.baseline_scores.items():
            current = current_scores[dim]
            if current < baseline - 5:  # 5점 이상 하락
                alerts.append(f"{dim}: {baseline:.1f} → {current:.1f} (⚠️)")
        
        return alerts
```

---

## 📈 품질 개선 사례

### 사례 1: 결측치 보완 (완전성 개선)

**Before**:
- 제조시설면적 결측: 15%
- 완전성 점수: 75점

**조치**:
```python
# 동일 자치구 내 중위값으로 대체
for gu in df['시군구명'].unique():
    mask = (df['시군구명'] == gu) & df['제조시설면적'].isnull()
    median_area = df[df['시군구명'] == gu]['제조시설면적'].median()
    df.loc[mask, '제조시설면적'] = median_area
```

**After**:
- 제조시설면적 결측: 2%
- 완전성 점수: 92점 (+17점)

---

### 사례 2: 중복 제거 (일관성 개선)

**Before**:
- 중복 레코드: 5%
- 일관성 점수: 70점

**조치**:
```python
# 공장관리번호 기준 중복 제거
df = df.drop_duplicates(subset=['공장관리번호'], keep='first')

# 유사 주소 병합
df['주소_정제'] = df['공장주소'].str.replace(r'\s+', '', regex=True)
df = df.drop_duplicates(subset=['주소_정제'], keep='first')
```

**After**:
- 중복 레코드: 0.5%
- 일관성 점수: 95점 (+25점)

---

## 🎯 품질 목표 설정

### 단계별 목표

#### 단기 (1개월)
- 완전성: 80점 이상
- 정확성: 75점 이상
- 중복 레코드 5% 미만

#### 중기 (3개월)
- 완전성: 90점 이상
- 정확성: 85점 이상
- 일관성: 90점 이상

#### 장기 (6개월)
- 모든 차원: 90점 이상
- 종합 점수: 90점 이상 (A등급)

---

## 📚 참고 자료

### 국제 표준
- ISO 8000: 데이터 품질 표준
- ISO 25012: 데이터 품질 모델
- DAMA-DMBOK: 데이터 관리 지식 체계

### 추천 도서
- "Data Quality: The Accuracy Dimension" - Jack E. Olson
- "데이터 품질 관리론" - 한국데이터산업진흥원

### 온라인 자료
- [AIRD Pack GitHub](https://github.com/your-org/aird-pack)
- [데이터 품질 가이드](https://www.data.go.kr/quality)

---

## 💡 Best Practices

### 1. 정기적 진단
- 월별 자동 품질진단 실행
- 분기별 심층 분석
- 연간 종합 평가

### 2. 문제 추적
- 발견된 문제 이슈 트래킹
- 개선 조치 문서화
- 효과 측정

### 3. 협업
- 데이터 생산자와 소통
- 이해관계자 피드백 수렴
- 개선 사항 공유

### 4. 자동화
- 품질 체크 자동화
- 알림 시스템 구축
- 대시보드 모니터링

---

## 🔧 문제 해결 가이드

### Q1: 품질 점수가 낮게 나왔습니다
**A**: 차원별 세부 점수를 확인하고, 가장 낮은 차원부터 개선하세요.

### Q2: 결측치가 너무 많습니다
**A**: 
1. 필수 컬럼인지 확인
2. 필수가 아니면 제외 고려
3. 필수면 데이터 수집 프로세스 개선

### Q3: 중복 레코드를 어떻게 처리하나요?
**A**:
1. 키 컬럼 기준 중복 확인
2. 최신 데이터 우선 keep
3. 병합 가능하면 병합

### Q4: 이상치를 모두 제거해야 하나요?
**A**: 
- 명백한 오류는 제거
- 극단값이지만 유효한 값은 유지
- 도메인 전문가와 상의

---

## 📞 지원

### 기술 지원
- 이메일: aird-support@example.com
- GitHub Issues
- 월간 Q&A 세션

### 교육 프로그램
- 품질진단 기초 (1일)
- 품질 개선 실무 (2일)
- 품질 관리 고급 (3일)

---

**AIRD Quality Diagnosis v1.0**  
**작성일**: 2025-11-28  
**문의**: aird-support@example.com
